<?php
function dnd($data) {
	echo "<pre>";
	print_r($data);
	die();
}

function sorting($array, $key, $wie) {

	/* $key = array_column($array, $key);
	if($wie == "DESC"){
		array_multisort($key, SORT_DESC, $array);
	} else {
		array_multisort($key, SORT_ASC, $array);
	} */
 return $array;
}
